import { CoordDisplayControl } from "./CoordDisplayControl.tsx";
import { ViewModeControl } from "./ViewModeControl.tsx";

/**
 * 每个控件的Key
 */
export enum ControlKeyEnum {
  COORD_CONTROL = "coord_control",
  VIEW_MODE_CONTROL = "view_mode_control",
}

export { CoordDisplayControl, ViewModeControl };
